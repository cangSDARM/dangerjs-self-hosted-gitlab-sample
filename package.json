{
  "name": "project",
  "version": "1.0.0",
  "description": "desc",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepush": "echo 'with husky' && npm danger:prepush",
    "danger:prepush": "env-cmd danger local --dangerfile dangerfile.js",
    "danger": "echo 'cicd running' && danger ci --failOnErrors -v",
    "local": "echo 'local test' && env-cmd danger pr https://gitlab.example.com/user/project/merge_requests/mrId"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.example.com:user/project.git"
  },
  "author": "",
  "devDependencies": {
    "danger": "10.5.4",
    "env-cmd": "^10.1.0"
  },
  "dependencies": {
    "husky": "^7.0.4",
    "lodash.find": "^4.6.0",
    "lodash.includes": "^4.3.0"
  }
}
